"""
Implementation of a converter that takes in input a chord annotated using
the ABC notation, and converts it into the Harte notation.
"""
import csv
import re
from typing import Tuple


def decompose_abc(abc_chord: str) -> Tuple:
    pass


if '__main__' == __name__:
    pass
